name: Publish @harmonograph/svg package on merge to master

on:
  push:
    paths:
    - 'packages/svg/**'
    branches:
    - master

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@master
    - run: |
        yarn
        yarn workspace @harmonograph/svg test
    - run: yarn workspace @harmonograph/svg publish
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
